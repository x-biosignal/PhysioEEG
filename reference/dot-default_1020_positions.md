# Default 10-20 electrode positions (2D projection)

Returns standard 10-20 system electrode positions projected onto a unit
circle for topographic mapping.

## Usage

``` r
.default_1020_positions()
```

## Value

A named list with pos_x and pos_y numeric vectors, with names
corresponding to 10-20 labels.
