# Generate head outline coordinates

Returns coordinates for a unit circle representing the head outline.

## Usage

``` r
.head_outline(n_points = 100)
```

## Arguments

- n_points:

  Number of points on the circle (default: 100).

## Value

A data.frame with columns x, y.
