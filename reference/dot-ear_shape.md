# Generate ear shape coordinates

Returns coordinates for a stylized ear on the specified side of the head
circle.

## Usage

``` r
.ear_shape(side = c("left", "right"))
```

## Arguments

- side:

  Character, either "left" or "right".

## Value

A data.frame with columns x, y.
