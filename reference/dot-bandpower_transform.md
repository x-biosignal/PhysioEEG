# Band-power (log-variance of band-filtered signal) features - fit-free

Band-power (log-variance of band-filtered signal) features - fit-free

## Usage

``` r
.bandpower_transform(data, idx, sr, bands)
```
