# Fold assignment (stratified k-fold or leave-one-group-out), group-aware

Fold assignment (stratified k-fold or leave-one-group-out), group-aware

## Usage

``` r
.decode_folds(labels, n_folds, groups, cv)
```
