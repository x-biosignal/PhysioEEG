% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-pac.R
\name{eegComodulogram}
\alias{eegComodulogram}
\title{Comodulogram (phase-frequency x amplitude-frequency PAC) for EEG}
\usage{
eegComodulogram(
  pe,
  channel = 1L,
  phase_freqs = seq(2, 14, by = 2),
  amp_freqs = seq(20, 100, by = 10),
  method = c("tort", "canolty", "ozkurt", "plv"),
  phase_bw = 2,
  amp_bw = 10,
  n_bins = 18L,
  assay_name = NULL
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment}.}

\item{channel}{Channel (index or label) to analyse.}

\item{phase_freqs}{Phase centre frequencies in Hz (default \code{seq(2, 14, 2)}).}

\item{amp_freqs}{Amplitude centre frequencies in Hz (default \code{seq(20, 100, 10)}).}

\item{method}{PAC estimator (see \code{\link[=eegPAC]{eegPAC()}}).}

\item{phase_bw, amp_bw}{Half-bandwidths in Hz (defaults 2 and 10).}

\item{n_bins}{Phase bins for the Tort modulation index (default 18).}

\item{assay_name}{Assay to use (default: the object's default assay).}
}
\value{
The comodulogram list from \code{PhysioCrossModal::comodulogram()} (a
\code{matrix} of phase x amplitude frequencies, plus its \code{peak}).
}
\description{
Computes phase-amplitude coupling over a grid of phase and amplitude
frequencies for one EEG channel, yielding a comodulogram whose peak locates
the dominant coupling. Delegates to \code{PhysioCrossModal::comodulogram()}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 7500, n_channels = 2, sr = 250)
cm <- eegComodulogram(pe, channel = 1)
cm$peak
}
}
\seealso{
\code{\link[=eegPAC]{eegPAC()}}
}
