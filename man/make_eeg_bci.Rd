% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-data.R
\name{make_eeg_bci}
\alias{make_eeg_bci}
\title{Create EEG with Motor Imagery / SSVEP Patterns for BCI}
\usage{
make_eeg_bci(n_trials = 30, n_channels = 8, sr = 256, trial_sec = 4)
}
\arguments{
\item{n_trials}{Number of trials per class (default: 30). Total trials
will be \code{2 * n_trials}.}

\item{n_channels}{Number of channels (default: 8).}

\item{sr}{Sampling rate in Hz (default: 256).}

\item{trial_sec}{Trial duration in seconds (default: 4.0).}
}
\value{
A PhysioExperiment with a 3D \code{"raw"} assay (time x channels x
total_trials). Sets \code{metadata(x)$labels} with a character vector
of class labels (\code{"left"} or \code{"right"}) for each trial.
}
\description{
Generates epoched EEG with two classes: left motor imagery (mu ERD over C4)
and right motor imagery (mu ERD over C3). Also includes SSVEP at specified
frequencies at occipital channels. Data is stored as a 3D array
(time x channels x trials).
}
\examples{
pe <- make_eeg_bci(n_trials = 5, n_channels = 4, sr = 128)
dim(SummarizedExperiment::assay(pe, "raw"))  # 512 x 4 x 10
}
\seealso{
\code{\link[=make_eeg]{make_eeg()}}, \code{\link[=eegCSP]{eegCSP()}}, \code{\link[=eegBCIfeatures]{eegBCIfeatures()}},
\code{\link[=eegBCIclassify]{eegBCIclassify()}}, \code{\link[=eegMotorImagery]{eegMotorImagery()}}
}
