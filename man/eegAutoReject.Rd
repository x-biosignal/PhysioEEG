% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-preprocess.R
\name{eegAutoReject}
\alias{eegAutoReject}
\title{Automated epoch rejection and interpolation (autoreject)}
\usage{
eegAutoReject(
  x,
  consensus = NULL,
  n_interpolate = NULL,
  cv_folds = 5L,
  thresh_range = NULL,
  n_thresh = 20L,
  assay_name = "epoched",
  output_assay = "autoreject_clean"
)
}
\arguments{
\item{x}{A PhysioExperiment with 3D epoched data (time x channels x epochs)
in \code{metadata(x)[[assay_name]]} (from \code{\link[=eegEpoch]{eegEpoch()}}) or in a 3D assay of
that name (from \code{\link[=make_eeg_erp]{make_eeg_erp()}}).}

\item{consensus}{Fraction of bad channels (from 0 to 1) above which an epoch
is dropped. \code{NULL} selects it by cross-validation.}

\item{n_interpolate}{Number of worst bad channels to interpolate per repaired
epoch. \code{NULL} selects it by cross-validation.}

\item{cv_folds}{Number of cross-validation folds (default: 5).}

\item{thresh_range}{Optional candidate peak-to-peak threshold grid: either a
length-2 \code{c(min, max)} span (expanded to \code{n_thresh} values) or an
explicit vector of candidate thresholds. \code{NULL} derives the span from
the data.}

\item{n_thresh}{Number of grid thresholds when \code{thresh_range} is
\code{NULL} or a span (default: 20).}

\item{assay_name}{Name of the 3D epoched data in metadata or assays
(default: \code{"epoched"}).}

\item{output_assay}{Name for the cleaned 3D data stored in metadata
(default: \code{"autoreject_clean"}).}
}
\value{
The PhysioExperiment with cleaned 3D epoched data in
\code{metadata(x)[[output_assay]]} (dropped epochs removed, repaired epochs
interpolated) and a rejection log in \code{metadata(x)$autoreject}: the
per-channel \code{thresholds}, chosen \code{consensus} and
\code{n_interpolate}, the epoch-by-channel \code{bad_matrix}, the
\code{interpolated} channel-epochs, and the \code{dropped_epochs}. The step
is recorded in provenance.
}
\description{
Implements the autoreject algorithm (Jas et al. 2017) for epoched EEG.
Per-channel peak-to-peak rejection thresholds are chosen by cross-validation,
then each epoch is kept, repaired by interpolating its worst bad channels, or
dropped, according to two hyperparameters: \code{consensus} (the fraction of
bad channels above which an epoch is dropped) and \code{n_interpolate} (the
number of worst bad channels to interpolate in a repaired epoch). Either
hyperparameter is chosen by cross-validation when not supplied. Bad channels
are repaired with spherical-spline interpolation (the same weights used by
\code{\link[=eegInterpolate]{eegInterpolate()}}), so electrode positions must be present (run
\code{\link[=eegMontage]{eegMontage()}} first).
}
\details{
The cross-validation objective, at every stage, is the root-mean-square error
between the mean of the good/cleaned training epochs and the median of the
held-out test epochs - the criterion introduced by autoreject.
}
\examples{
\dontrun{
pe <- make_eeg_erp(n_epochs = 40, n_channels = 19, sr = 250)
pe <- eegMontage(pe, system = "10-20")
pe <- eegAutoReject(pe, assay_name = "raw")
dim(S4Vectors::metadata(pe)$autoreject$bad_matrix)
}
}
\references{
Jas, M., Engemann, D. A., Bekhti, Y., Raimondo, F., & Gramfort, A. (2017).
Autoreject: Automated artifact rejection for MEG and EEG data. NeuroImage,
159, 417-429.
}
\seealso{
\code{\link[=eegArtifactReject]{eegArtifactReject()}}, \code{\link[=eegInterpolate]{eegInterpolate()}}, \code{\link[=eegEpoch]{eegEpoch()}},
\code{\link[=eegMontage]{eegMontage()}}
}
