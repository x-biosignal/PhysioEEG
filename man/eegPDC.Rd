% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-connectivity.R
\name{eegPDC}
\alias{eegPDC}
\title{Partial Directed Coherence (PDC)}
\usage{
eegPDC(
  x,
  order = NULL,
  freqs = NULL,
  generalized = TRUE,
  band = NULL,
  method = "ols",
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with 2D EEG data (time x channels).}

\item{order}{MVAR model order, or \code{NULL} to select automatically.}

\item{freqs}{Numeric vector of frequencies in Hz (default: 128 points from 0
to the Nyquist frequency).}

\item{generalized}{Use generalized PDC (default: \code{TRUE}).}

\item{band}{Optional numeric length-2 band in Hz over which to average the
stored connectivity matrix (default: all frequencies).}

\item{method}{MVAR estimator passed to \code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}
(default: \code{"ols"}).}

\item{assay_name}{Input assay name (default: the default assay).}
}
\value{
The PhysioExperiment with \code{metadata(x)$connectivity} set as in
\code{\link[=eegDTF]{eegDTF()}} (a band-averaged directed \code{matrix}, the frequency-resolved
\code{array}, \code{frequencies}, and settings).
}
\description{
Estimates frequency-resolved directed connectivity with Partial Directed
Coherence (Baccala & Sameshima 2001), computed from the frequency-domain
coefficient matrix \eqn{\bar{A}(f)} of an MVAR model
(\code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}). Unlike DTF, PDC reflects only \emph{direct}
channel-to-channel influences, so a purely indirect pathway gives PDC near
zero. The (default) generalized PDC weights each row by the inverse residual
standard deviation to make the measure scale-invariant (Baccala 2007). PDC
satisfies \eqn{\sum_i \mathrm{PDC}_{ij}(f)^2 = 1} for each source \eqn{j}
(outflow normalization).
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 4000, n_channels = 5, sr = 250)
pe <- eegPDC(pe, order = 5)
metadata(pe)$connectivity$matrix
}
}
\references{
Baccala, L. A., & Sameshima, K. (2001). Partial directed coherence: a new
concept in neural structure determination. Biological Cybernetics, 84(6),
463-474.
}
\seealso{
\code{\link[=eegDTF]{eegDTF()}}, \code{\link[=eegConditionalGC]{eegConditionalGC()}}, \code{\link[=eegConnectivityMatrix]{eegConnectivityMatrix()}}
}
