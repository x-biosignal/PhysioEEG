% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotButterflyGFP}
\alias{eegPlotButterflyGFP}
\title{Plot EEG Butterfly Traces with Global Field Power}
\usage{
eegPlotButterflyGFP(
  x,
  channels = NULL,
  time_range = NULL,
  show_events = FALSE,
  assay_name = NULL,
  gfp_definition = c("population", "sample"),
  heights = c(3, 1)
)
}
\arguments{
\item{x}{A PhysioExperiment object containing a numeric 2D continuous EEG
assay (time x channels).}

\item{channels}{Unique character vector of exact channel labels. If
\code{NULL}, all channels are shown.}

\item{time_range}{Optional finite increasing length-two vector specifying
the inclusive display range in seconds. GFP is calculated before display
windowing.}

\item{show_events}{Logical; draw finite event times from
\code{metadata(x)$events} on both panels.}

\item{assay_name}{Input assay name. If \code{NULL}, uses the default assay.}

\item{gfp_definition}{Denominator used for spatial dispersion:
\code{"population"} divides by C and matches \code{eegMicrostates()};
\code{"sample"} divides by C - 1.}

\item{heights}{Two finite positive values giving the butterfly and GFP
panel height ratio.}
}
\value{
A patchwork object containing two ggplot panels.
}
\description{
Displays the selected continuous EEG channels as a butterfly plot above
their Global Field Power (GFP). GFP is the spatial dispersion across the
displayed channels at each time point, so its magnitude depends on the
selected channels and EEG reference. It is not source-localized power.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 2500, n_channels = 8, sr = 500)
eegPlotButterflyGFP(pe, channels = c("Fz", "Cz", "Pz"))
}
}
