% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-complexity.R
\name{eegComplexity}
\alias{eegComplexity}
\title{EEG complexity and nonlinear-dynamics measures}
\usage{
eegComplexity(
  pe,
  measures = c("sample_entropy", "permutation_entropy", "lempel_ziv", "higuchi_fd",
    "dfa", "hjorth_mobility", "spectral_entropy"),
  assay_name = NULL,
  m = 2L,
  r = 0.2,
  tau = 1L,
  perm_order = 3L,
  mse_scales = 1:8,
  max_samples = 4000L
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment}.}

\item{measures}{Character vector of measures to compute (see Details).}

\item{assay_name}{Assay to use (default: the object's default assay).}

\item{m}{Embedding dimension for the entropy measures (default 2; permutation
entropy uses \code{perm_order}).}

\item{r}{Tolerance as a fraction of each channel's SD for sample/approximate/
multiscale entropy (default 0.2).}

\item{tau}{Time delay for permutation entropy (default 1).}

\item{perm_order}{Embedding order for permutation entropy (default 3).}

\item{mse_scales}{Scales for multiscale entropy (default \code{1:8}).}

\item{max_samples}{Cap on samples per channel/epoch for the O(N^2) entropy
measures; longer series are truncated with a warning (default 4000; \code{NULL}
disables the cap).}
}
\value{
A data frame with one row per channel and one column per requested
measure (plus a \code{channel} column). The multiscale-entropy per-scale curves
are attached as \code{attr(, "mse_scales")} / \code{attr(, "mse_curve")}.
}
\description{
Computes per-channel entropy, fractal-dimension and detrended-fluctuation
complexity measures — the nonlinear family that complements the spectral,
connectivity and source tools. For epoched (3-D) data each measure is computed
per epoch and averaged across epochs.
}
\details{
Available \code{measures}: \code{"sample_entropy"} (Richman–Moorman), \code{"approximate_entropy"}
(Pincus), \code{"permutation_entropy"} (Bandt–Pompe), \code{"multiscale_entropy"} (Costa;
returns the scale-averaged SampEn as a summary plus the per-scale curve in the
attribute), \code{"lempel_ziv"} (Kaspar–Schuster), \code{"higuchi_fd"}, \code{"katz_fd"},
\code{"dfa"} (scaling exponent alpha), \code{"hurst"} (R/S), \code{"hjorth_mobility"},
\code{"hjorth_complexity"}, \code{"spectral_entropy"}.
}
\examples{
pe <- make_eeg(n_time = 512, n_channels = 4, sr = 128)
cx <- eegComplexity(pe, measures = c("permutation_entropy", "hjorth_mobility",
                                     "spectral_entropy"))
cx
}
\references{
Richman & Moorman 2000; Bandt & Pompe 2002; Costa 2002; Higuchi
1988; Katz 1988; Peng 1994; Hjorth 1970.
}
\seealso{
\code{\link[=eegQEEG]{eegQEEG()}}, \code{\link[=eegAperiodic]{eegAperiodic()}}
}
