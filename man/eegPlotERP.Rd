% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotERP}
\alias{eegPlotERP}
\title{Plot ERP Waveform with Confidence Interval}
\usage{
eegPlotERP(
  x,
  channels = NULL,
  conditions = NULL,
  ci = 0.95,
  show_ci = TRUE,
  epoch_start = 0,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with epoched (3D) EEG data
(time x channels x epochs).}

\item{channels}{Character vector of channel labels to plot. If \code{NULL},
the grand average across all channels is used.}

\item{conditions}{Character vector of condition labels to include.
If \code{NULL} and \code{metadata(x)$conditions} exists, all conditions
are plotted.}

\item{ci}{Confidence level for the interval (default: 0.95).}

\item{show_ci}{Logical; if \code{TRUE}, display confidence interval ribbon.}

\item{epoch_start}{Numeric start time of each epoch in seconds for
the x-axis (default: 0).}

\item{assay_name}{Input assay name. If \code{NULL}, uses the default assay.}
}
\value{
A ggplot2 object.
}
\description{
Plots event-related potential waveforms averaged across epochs, with
optional confidence interval ribbons. Supports condition-based
comparisons when \code{metadata(x)$conditions} is available.
}
\examples{
\dontrun{
pe <- make_eeg_erp(n_epochs = 40, sr = 250)
eegPlotERP(pe, channels = "Cz")
}
}
