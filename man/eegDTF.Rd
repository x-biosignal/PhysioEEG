% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-connectivity.R
\name{eegDTF}
\alias{eegDTF}
\title{Directed Transfer Function (DTF)}
\usage{
eegDTF(
  x,
  order = NULL,
  freqs = NULL,
  normalized = TRUE,
  ffDTF = FALSE,
  band = NULL,
  method = "ols",
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with 2D EEG data (time x channels).}

\item{order}{MVAR model order, or \code{NULL} to select automatically.}

\item{freqs}{Numeric vector of frequencies in Hz (default: 128 points from 0
to the Nyquist frequency).}

\item{normalized}{Row-normalize the DTF (default: \code{TRUE}).}

\item{ffDTF}{Use the full-frequency DTF normalization (default: \code{FALSE}).}

\item{band}{Optional numeric length-2 band in Hz over which to average the
stored connectivity matrix (default: all frequencies).}

\item{method}{MVAR estimator passed to \code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}
(default: \code{"ols"}).}

\item{assay_name}{Input assay name (default: the default assay).}
}
\value{
The PhysioExperiment with \code{metadata(x)$connectivity} set to a
list containing the band-averaged directed \code{matrix} (rows = targets,
columns = sources), the frequency-resolved \code{array}
(n_channels x n_channels x n_freqs), the \code{frequencies}, and the
settings used.
}
\description{
Estimates frequency-resolved directed connectivity between EEG channels with
the Directed Transfer Function (Kaminski & Blinowska 1991), computed from the
transfer function \eqn{H(f)} of a multivariate autoregressive (MVAR) model
fitted with \code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}. Because \eqn{H(f)} inverts the whole VAR
system, DTF reflects both direct and indirect pathways. The (default)
normalized DTF satisfies \eqn{\sum_j \mathrm{DTF}_{ij}(f)^2 = 1} for each
target \eqn{i} (inflow normalization); \code{ffDTF} normalizes across all
frequencies instead (Korzeniewska 2003).
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 4000, n_channels = 5, sr = 250)
pe <- eegDTF(pe, order = 5)
dim(metadata(pe)$connectivity$array)
}
}
\references{
Kaminski, M. J., & Blinowska, K. J. (1991). A new method of the description
of the information flow in the brain structures. Biological Cybernetics,
65(3), 203-210.
}
\seealso{
\code{\link[=eegPDC]{eegPDC()}}, \code{\link[=eegConditionalGC]{eegConditionalGC()}}, \code{\link[=eegConnectivityMatrix]{eegConnectivityMatrix()}},
\code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}
}
