% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotConnectivity}
\alias{eegPlotConnectivity}
\title{Plot Connectivity Matrix or Circle}
\usage{
eegPlotConnectivity(
  x,
  method = c("heatmap", "circle"),
  matrix = NULL,
  threshold = 0,
  labels = NULL,
  palette = "RdBu"
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{method}{Display method: \code{"heatmap"} for a correlation/connectivity
matrix heatmap or \code{"circle"} for a circular connectivity diagram.}

\item{matrix}{Numeric matrix of connectivity values. If \code{NULL},
reads from \code{metadata(x)$connectivity$matrix}.}

\item{threshold}{Numeric; only show connections above this value
(default: 0).}

\item{labels}{Character vector of channel labels. If \code{NULL}, uses
row/column names of the matrix or channel labels from colData.}

\item{palette}{Character name of the diverging color palette
(default: \code{"RdBu"}).}
}
\value{
A ggplot2 object.
}
\description{
Visualizes EEG connectivity as either a heatmap (matrix) or a circular
connectivity plot (circle). The connectivity matrix can be provided
directly or read from \code{metadata(x)$connectivity$matrix}.
}
\examples{
\dontrun{
mat <- matrix(runif(16), 4, 4)
diag(mat) <- 1
pe <- make_eeg(n_time = 1000, n_channels = 4, sr = 250)
eegPlotConnectivity(pe, method = "heatmap", matrix = mat)
}
}
