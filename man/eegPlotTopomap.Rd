% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotTopomap}
\alias{eegPlotTopomap}
\title{Plot EEG Topographic Map}
\usage{
eegPlotTopomap(
  x,
  time = NULL,
  values = NULL,
  assay_name = NULL,
  resolution = 100,
  palette = "RdBu",
  contours = TRUE,
  electrodes = TRUE
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{time}{Numeric time point in seconds at which to extract values
from the assay. If \code{NULL} and \code{values} is also \code{NULL},
the mean across all time points is used.}

\item{values}{Named numeric vector of channel values to plot directly.
If provided, overrides data extraction from the assay.}

\item{assay_name}{Input assay name. If \code{NULL}, uses the default assay.}

\item{resolution}{Integer grid resolution for interpolation (default: 100).}

\item{palette}{Character name of the diverging color palette
(default: \code{"RdBu"}).}

\item{contours}{Logical; if \code{TRUE}, add contour lines.}

\item{electrodes}{Logical; if \code{TRUE}, show electrode positions as points.}
}
\value{
A ggplot2 object.
}
\description{
Creates a 2D scalp topographic map using inverse distance weighted
interpolation. Electrode positions are read from \code{colData(x)}
(columns \code{pos_x}, \code{pos_y}) or default 10-20 positions are
used as fallback. The plot includes a head outline, nose, and ears.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 500, n_channels = 19, sr = 250)
eegPlotTopomap(pe, time = 0.5)
}
}
