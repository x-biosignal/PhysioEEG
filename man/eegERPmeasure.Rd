% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-erp.R
\name{eegERPmeasure}
\alias{eegERPmeasure}
\title{Measure ERP Amplitude}
\usage{
eegERPmeasure(
  x,
  window,
  method = c("peak", "mean", "adaptive_mean"),
  polarity = c("positive", "negative"),
  epoch_start = 0,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{window}{Numeric vector of length 2: \code{c(start_ms, end_ms)}.}

\item{method}{Measurement method: \code{"peak"} (peak amplitude),
\code{"mean"} (mean amplitude), or \code{"adaptive_mean"} (mean in
+/-25ms around peak).}

\item{polarity}{Expected polarity: \code{"positive"} or \code{"negative"}.}

\item{epoch_start}{Start time of the epoch in milliseconds relative to
stimulus onset. Default is 0 (epoch starts at stimulus).}

\item{assay_name}{Input assay name (default: first assay).}
}
\value{
A data.frame with columns: \code{channel} (character label),
\code{amplitude} (numeric measured amplitude), \code{latency_ms}
(numeric latency in milliseconds), and \code{method} (character
measurement method used).
}
\description{
Measures ERP amplitude in a specified time window using peak, mean, or
adaptive mean methods. For epoched (3D) data, averages across epochs first.
}
\examples{
\dontrun{
pe <- make_eeg_erp(n_epochs = 40, sr = 250)
result <- eegERPmeasure(pe, window = c(250, 500), method = "peak",
                        polarity = "positive")
}
}
\references{
Luck, S. J. (2014). An Introduction to the Event-Related Potential Technique
(2nd ed.). MIT Press.
}
\seealso{
\code{\link[=eegERPdetect]{eegERPdetect()}}, \code{\link[=eegERPlatency]{eegERPlatency()}}, \code{\link[=eegERPbaseline]{eegERPbaseline()}},
\code{\link[=eegPlotERP]{eegPlotERP()}}
}
