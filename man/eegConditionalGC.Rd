% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-connectivity.R
\name{eegConditionalGC}
\alias{eegConditionalGC}
\title{Conditional (multivariate) Granger causality}
\usage{
eegConditionalGC(
  x,
  target,
  source,
  conditioning = NULL,
  order = NULL,
  method = "ols",
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with 2D EEG data (time x channels).}

\item{target}{Target channel (label or index) - the effect.}

\item{source}{Source channel (label or index) - the putative cause.}

\item{conditioning}{Channels to condition on (labels or indices). Defaults to
all remaining channels.}

\item{order}{MVAR model order, or \code{NULL} to select automatically on the
full channel set.}

\item{method}{MVAR estimator passed to \code{\link[PhysioCore:mvarFit]{PhysioCore::mvarFit()}}
(default: \code{"ols"}).}

\item{assay_name}{Input assay name (default: the default assay).}
}
\value{
A list with the conditional GC \code{value} (in nats, non-negative in
theory), the \code{target}, \code{source}, and \code{conditioning} labels,
and the VAR \code{order} used.
}
\description{
Computes conditional Granger causality from \code{source} to \code{target}
given a set of \code{conditioning} channels, using the log ratio of the
target's residual variance in a VAR fitted without the source
(\code{target + conditioning}) versus with it
(\code{target + source + conditioning}); both VARs share the same order
(Geweke; Barnett & Seth 2014). Unlike the pairwise \code{\link[=eegGrangerCausality]{eegGrangerCausality()}},
conditioning removes indirect influences, so for a chain the conditional GC
along a purely indirect path is near zero.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 4000, n_channels = 3, sr = 250)
eegConditionalGC(pe, target = 3, source = 1, conditioning = 2)
}
}
\references{
Barnett, L., & Seth, A. K. (2014). The MVGC multivariate Granger causality
toolbox. Journal of Neuroscience Methods, 223, 50-68.
}
\seealso{
\code{\link[=eegGrangerCausality]{eegGrangerCausality()}}, \code{\link[=eegDTF]{eegDTF()}}, \code{\link[=eegPDC]{eegPDC()}}
}
