% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-complexity.R
\name{multiscaleEntropy}
\alias{multiscaleEntropy}
\title{Multiscale Entropy}
\usage{
multiscaleEntropy(x, m = 2L, r = 0.2, scales = 1:8)
}
\arguments{
\item{x}{A numeric vector (the time series).}

\item{m}{Embedding dimension for the sample entropy (default 2).}

\item{r}{Tolerance as a fraction of the ORIGINAL signal's SD (default 0.2),
held fixed across all scales (Costa's convention).}

\item{scales}{Integer vector of time scales (default \code{1:8}).}
}
\value{
A named numeric vector of sample entropies, one per scale
(\code{scale1}, \code{scale2}, ...). \code{NA} for a scale whose
coarse-grained series is too short or yields no template matches.
}
\description{
Computes the multiscale entropy (MSE; Costa, Goldberger & Peng, 2002) of a
one-dimensional signal: the sample entropy of the signal after coarse-graining
it at a series of time scales. At scale \eqn{s} the signal is partitioned into
non-overlapping windows of length \eqn{s}, each window is replaced by its mean,
and the sample entropy (embedding \code{m}, tolerance \code{r} times the SD of
the ORIGINAL signal, held fixed across scales) of that coarse-grained series is
the MSE at scale \eqn{s}. A signal whose MSE stays high or rises at coarse
scales carries structure across time scales; white noise, by contrast, has an
MSE that falls monotonically from scale 1.
}
\examples{
set.seed(1); multiscaleEntropy(rnorm(1000), scales = 1:5)  # noise: falls with scale
}
\references{
Costa, M., Goldberger, A. L., & Peng, C.-K. (2002). "Multiscale entropy
analysis of complex physiologic time series." \emph{Physical Review Letters},
89(6), 068102. \doi{10.1103/PhysRevLett.89.068102}
}
\seealso{
\code{\link{eegComplexity}} for the multi-measure wrapper;
PhysioMoCap's \code{sampleEntropy} for the single-scale measure.
}
