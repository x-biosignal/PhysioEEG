% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-source.R
\name{eegSourceEstimate}
\alias{eegSourceEstimate}
\title{EEG Source Estimation}
\usage{
eegSourceEstimate(
  x,
  forward_model,
  method = c("sloreta", "eloreta", "mne"),
  lambda = 0.05,
  assay_name = NULL,
  output_assay = "source"
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{forward_model}{A forward model list as returned by
\code{\link{eegForwardModel}}.}

\item{method}{Source estimation method: \code{"sloreta"} (standardized
low-resolution tomography), \code{"eloreta"} (exact low-resolution
tomography), or \code{"mne"} (minimum norm estimate).}

\item{lambda}{Regularization parameter (default: 0.05). Higher values
produce smoother solutions.}

\item{assay_name}{Name of the input assay. If \code{NULL}, the default
assay is used.}

\item{output_assay}{Name for the output assay containing source estimates
(default: \code{"source"}).}
}
\value{
Modified PhysioExperiment with source estimates stored in
\code{output_assay}. The assay is a matrix of dimensions
n_time x (n_sources * 3). Sets \code{metadata(x)$source_estimate}
with a list containing: \code{method}, \code{lambda},
\code{n_sources}, and \code{n_source_cols}.
}
\description{
Estimates brain source activity from scalp EEG data using distributed
source imaging methods. Requires a forward model from
\code{\link{eegForwardModel}}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 500, n_channels = 19, sr = 250)
fm <- eegForwardModel(pe, method = "spherical", n_sources = 50)
pe <- eegSourceEstimate(pe, fm, method = "mne")
}
}
\references{
Pascual-Marqui, R. D. (2002). Standardized low-resolution brain electromagnetic
tomography (sLORETA). Methods and Findings in Experimental and Clinical
Pharmacology, 24(Suppl D), 5-12.
}
\seealso{
\code{\link[=eegForwardModel]{eegForwardModel()}}, \code{\link[=eegBeamformer]{eegBeamformer()}}, \code{\link[=eegSourcePower]{eegSourcePower()}},
\code{\link[=eegPlotSource]{eegPlotSource()}}
}
