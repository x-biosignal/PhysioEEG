% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotHypnogram}
\alias{eegPlotHypnogram}
\title{Plot Sleep Hypnogram}
\usage{
eegPlotHypnogram(x, stages = NULL, epoch_sec = 30, colors = NULL)
}
\arguments{
\item{x}{A PhysioExperiment object (used for metadata access).}

\item{stages}{A data.frame with columns \code{epoch} and \code{stage},
or \code{NULL} to read from \code{metadata(x)$sleep_stages}.}

\item{epoch_sec}{Epoch duration in seconds (default: 30).}

\item{colors}{Named character vector of colors for each stage.
If \code{NULL}, default colors are used.}
}
\value{
A ggplot2 object.
}
\description{
Displays a sleep hypnogram showing sleep stage transitions over time.
Stages are ordered with Wake at the top and N3 at the bottom, with
a characteristic staircase pattern.
}
\examples{
\dontrun{
pe <- make_eeg_sleep(n_time = 150000, n_channels = 2, sr = 500)
stages <- eegSleepStage(pe)
metadata(pe)$sleep_stages <- stages
eegPlotHypnogram(pe)
}
}
