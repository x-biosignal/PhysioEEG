% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-erp.R
\name{eegERPlatency}
\alias{eegERPlatency}
\title{Fractional Area Latency}
\usage{
eegERPlatency(
  x,
  window,
  fraction = 0.5,
  polarity = c("positive", "negative"),
  epoch_start = 0,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{window}{Numeric vector of length 2: \code{c(start_ms, end_ms)}.}

\item{fraction}{Fraction of total area (default: 0.5 for median latency).}

\item{polarity}{Expected polarity: \code{"positive"} or \code{"negative"}.}

\item{epoch_start}{Start time of the epoch in milliseconds relative to
stimulus onset. Default is 0 (epoch starts at stimulus).}

\item{assay_name}{Input assay name (default: first assay).}
}
\value{
A data.frame with columns: \code{channel} (character label),
\code{latency_ms} (numeric fractional area latency in milliseconds),
and \code{fraction} (numeric fraction used).
}
\description{
Computes the fractional area latency of an ERP component. This is the time
point at which a specified fraction of the total area under the curve (in
the given window) has accumulated.
}
\examples{
\dontrun{
pe <- make_eeg_erp(n_epochs = 40, sr = 250)
result <- eegERPlatency(pe, window = c(250, 500), fraction = 0.5,
                        polarity = "positive")
}
}
\references{
Luck, S. J. (2014). An Introduction to the Event-Related Potential Technique
(2nd ed.). MIT Press.
}
\seealso{
\code{\link[=eegERPdetect]{eegERPdetect()}}, \code{\link[=eegERPmeasure]{eegERPmeasure()}}, \code{\link[=eegERPbaseline]{eegERPbaseline()}},
\code{\link[=eegPlotERP]{eegPlotERP()}}
}
