% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotICA}
\alias{eegPlotICA}
\title{Plot ICA Components}
\usage{
eegPlotICA(
  x,
  components = NULL,
  time_range = NULL,
  show_topography = TRUE,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with ICA results.}

\item{components}{Integer vector of component indices to display.
If \code{NULL}, the first 10 (or fewer) are shown.}

\item{time_range}{Numeric vector of length 2 specifying time range in
seconds. If \code{NULL}, all data is shown.}

\item{show_topography}{Logical; if \code{TRUE} and ICA mixing matrix is
available in \code{metadata(x)$ica$mixing}, include topographic
inset labels.}

\item{assay_name}{Input assay name. If \code{NULL}, uses \code{"ica"}
if available, otherwise the default assay.}
}
\value{
A ggplot2 object.
}
\description{
Displays ICA component time courses in a stacked layout similar to
\code{eegPlotSignal}. Optionally shows topographic maps of the mixing
matrix weights as a side panel annotation.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, n_channels = 19, sr = 500)
pe <- eegICA(pe, n_components = 10)
eegPlotICA(pe, components = 1:5)
}
}
