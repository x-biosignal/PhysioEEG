% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-vis.R
\name{eegPlotGlassBrain}
\alias{eegPlotGlassBrain}
\title{Plot Source Localization as Multi-View Glass-Brain Projections}
\usage{
eegPlotGlassBrain(
  x,
  source_data = NULL,
  views = c("sagittal", "axial", "coronal"),
  threshold_pct = 90
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{source_data}{Optional explicit source data. Supply a data frame with
finite \code{x}, \code{y}, \code{z}, and \code{amplitude} columns, or a
structured list with \code{positions} and declared amplitude/matrix
reduction fields. If \code{NULL}, uses coordinate-bearing metadata from a
new \code{eegSourceEstimate()} or \code{eegBeamformer()} result.}

\item{views}{Non-empty unique character vector drawn exactly from
\code{"sagittal"}, \code{"axial"}, and \code{"coronal"}. Request order is
preserved.}

\item{threshold_pct}{Finite percentile in \code{[0, 100]}. The threshold is
the type-8 quantile of absolute amplitude over the complete source set;
ties are retained.}
}
\value{
A patchwork object for normal projections. When fewer than three
rows survive thresholding, returns the first requested view as an
\code{eegPlotSource()} ggplot fallback without applying a second
threshold. Immutable \code{"glassbrain_data"} attributes retain the
resolved sources, panel data, display transform, and threshold settings.
}
\description{
Creates deterministic sagittal, axial, and coronal maximum-intensity
projections from source amplitudes with real three-dimensional coordinates.
The bundled outline is schematic and is not an MRI, patient anatomy, or a
validation of the inverse solution.
}
\details{
Projection axes are sagittal \code{y-z} with depth \code{x}, axial
\code{x-y} with depth \code{z}, and coronal \code{x-z} with depth
\code{y}. Free-orientation source estimates are reduced by root-sum-square
over orientations at each time and RMS over time. This non-negative
summary is not a signed instantaneous current. A fixed 80 by 80 display
grid retains the greatest absolute amplitude in each projected bin, with
exact ties resolved by the smallest source ID. The affine display
transform comes from the complete unthresholded coordinate cloud and does
not modify amplitudes. The bundled outline is schematic: the plot is not
MRI registration, patient anatomy, or validation of localization accuracy.
Bare amplitude vectors and two-dimensional coordinates are rejected.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 500, n_channels = 19, sr = 250)
fm <- eegForwardModel(pe, method = "spherical", n_sources = 50)
localized <- eegSourceEstimate(pe, fm, method = "mne")
eegPlotGlassBrain(localized)
}
}
\seealso{
\code{\link[=eegForwardModel]{eegForwardModel()}}, \code{\link[=eegSourceEstimate]{eegSourceEstimate()}}, \code{\link[=eegBeamformer]{eegBeamformer()}},
\code{\link[=eegPlotSource]{eegPlotSource()}}
}
