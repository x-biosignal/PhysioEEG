% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-csd.R
\name{eegSurfaceLaplacian}
\alias{eegSurfaceLaplacian}
\title{Surface Laplacian (current source density) of EEG}
\usage{
eegSurfaceLaplacian(
  x,
  order = 4L,
  lambda = 1e-05,
  n_terms = 50L,
  assay_name = NULL,
  output_assay = "csd"
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}.}

\item{order}{Spline flexibility \code{m} (default 4; higher = stiffer).}

\item{lambda}{Smoothing / regularization added to the spline system (default
\code{1e-5}).}

\item{n_terms}{Number of Legendre terms in the g/h series (default 50).}

\item{assay_name}{Assay to transform (default: the object's default assay).}

\item{output_assay}{Name of the assay to store the CSD in (default \code{"csd"}).}
}
\value{
The \code{PhysioExperiment} with the CSD stored in \code{output_assay} (same
shape as the input assay; units are µV/m² up to a scale/head-radius
constant).
}
\description{
Applies the spherical-spline surface Laplacian (current source density, CSD)
of Perrin et al. (1989). CSD is \strong{reference-free} and acts as a spatial
high-pass filter, deblurring volume conduction so that each channel reflects
the radial current under it rather than the whole-head average — sharpening
focal topographies and attenuating broadly distributed activity. It needs
electrode positions (from \code{colData} columns \code{pos_x/pos_y/pos_z}, else matched
to a standard 10-10/10-20 montage by channel label).
}
\examples{
pe <- make_eeg(n_time = 500, n_channels = 19, sr = 250)
pe <- eegSurfaceLaplacian(pe)
"csd" \%in\% SummarizedExperiment::assayNames(pe)
}
\references{
Perrin, Pernier, Bertrand & Echallier (1989), Electroenceph Clin
Neurophysiol; Kayser & Tenke (2006), Clin Neurophysiol.
}
\seealso{
\code{\link[=eegRereference]{eegRereference()}}, \code{\link[=eegPlotTopomap]{eegPlotTopomap()}}, \code{\link[=eegConnectivityMatrix]{eegConnectivityMatrix()}}
}
