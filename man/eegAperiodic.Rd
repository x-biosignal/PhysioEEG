% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-aperiodic.R
\name{eegAperiodic}
\alias{eegAperiodic}
\title{Aperiodic (1/f) spectral parameterization of EEG}
\usage{
eegAperiodic(
  pe,
  freq_range = c(1, 45),
  aperiodic_mode = c("fixed", "knee"),
  max_n_peaks = 6L,
  peak_width_limits = c(1, 12),
  min_peak_height = 0.05,
  peak_threshold = 2,
  assay_name = NULL
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment}.}

\item{freq_range}{Frequency range to fit, in Hz (default \code{c(1, 45)}).}

\item{aperiodic_mode}{\code{"fixed"} (offset + exponent) or \code{"knee"} (offset +
knee + exponent), the latter for spectra with a bend in log-log space.}

\item{max_n_peaks}{Maximum number of oscillatory peaks per channel (default 6).}

\item{peak_width_limits}{Min/max peak width in Hz (default \code{c(1, 12)}).}

\item{min_peak_height}{Minimum peak height above the aperiodic fit (default 0.05).}

\item{peak_threshold}{Peak detection threshold in SD of the flattened spectrum
(default 2).}

\item{assay_name}{Assay to use (default: the object's default assay).}
}
\value{
An \code{eeg_aperiodic} object: a list with \code{aperiodic} (per-channel data
frame: \code{channel}, \code{exponent}, \code{offset}, optionally \code{knee}, \code{r_squared},
\code{error}), \code{peaks} (per-channel \code{CF}/\code{PW}/\code{BW}), \code{exponent} (a named
per-channel vector, e.g. for \code{\link[=eegPlotTopomap]{eegPlotTopomap()}}), and the underlying
\code{specparam_result}.
}
\description{
Separates each channel's power spectrum into an \strong{aperiodic} (1/f) component
and \strong{periodic} (oscillatory) peaks, following the specparam / FOOOF model
(Donoghue et al. 2020). The aperiodic exponent is a widely used index of the
excitation/inhibition balance and cortical state. Delegates the fit to
\code{PhysioAnalysis::specparam()}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 2500, n_channels = 8, sr = 250)
ap <- eegAperiodic(pe, freq_range = c(2, 40))
ap$aperiodic          # per-channel exponent / offset
}
}
\references{
Donoghue et al. 2020, Nat Neurosci (specparam / FOOOF).
}
\seealso{
\code{\link[=eegQEEG]{eegQEEG()}}, \code{\link[=eegComplexity]{eegComplexity()}}
}
