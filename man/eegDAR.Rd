% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-clinical.R
\name{eegDAR}
\alias{eegDAR}
\title{Delta/Alpha Ratio (DAR)}
\usage{
eegDAR(
  x,
  delta = c(1, 4),
  alpha = c(8, 13),
  regions = NULL,
  method = "welch",
  age = NULL,
  montage = NULL,
  reliability = list(icc = NA_real_, sem = NA_real_),
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with 2D EEG data.}

\item{delta}{Numeric length-2 delta band in Hz (default: \code{c(1, 4)}).}

\item{alpha}{Numeric length-2 alpha band in Hz (default: \code{c(8, 13)}).}

\item{regions}{Optional named list mapping a region name to a character
vector of channel labels; band powers are averaged within each region. When
\code{NULL} (default) one biomarker is returned per channel.}

\item{method}{Spectral estimator passed to the band-power routine
(default: \code{"welch"}).}

\item{age}{Optional numeric age used to select the normative reference row.}

\item{montage}{Optional character montage for the normative lookup.}

\item{reliability}{Named list of reliability indices to attach (default:
\code{icc} and \code{sem} placeholders).}

\item{assay_name}{Input assay name (default: the default assay).}
}
\value{
A named list of \code{\link[PhysioCore]{PhysioBiomarker}} objects, one
per channel or region.
}
\description{
Computes the delta-to-alpha band-power ratio per channel (or per region), a
quantitative-EEG stroke marker whose elevation tracks ischaemic slowing
(Finnigan et al. 2016). Band powers use a Welch estimator for a stable
spectrum. Each ratio is returned as a reliability-characterised
\code{\link[PhysioCore]{physioBiomarker}} carrying provenance (band, method,
software version), reliability placeholders, and - when age (and montage) are
supplied - a published reference range from
\code{\link[PhysioCore]{normativeLookup}} so the value prints its normative
percentile.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, n_channels = 19, sr = 250)
dar <- eegDAR(pe, age = 60)
dar[["C3"]]
}
}
\references{
Finnigan, S., Wong, A., & Read, S. (2016). Defining abnormal slow EEG
activity in acute ischaemic stroke: Delta/alpha ratio as an optimal QEEG
index. Clinical Neurophysiology, 127(2), 1452-1459.
}
\seealso{
\code{\link[=eegBSI]{eegBSI()}}, \code{\link[=eegSlowing]{eegSlowing()}}, \code{\link[=eegQEEG]{eegQEEG()}},
\code{\link[PhysioCore:physioBiomarker-constructor]{PhysioCore::physioBiomarker()}}, \code{\link[PhysioCore:normativeLookup]{PhysioCore::normativeLookup()}}
}
