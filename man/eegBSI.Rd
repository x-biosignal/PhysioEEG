% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-clinical.R
\name{eegBSI}
\alias{eegBSI}
\title{Brain Symmetry Index (BSI) and pairwise-derived pdBSI}
\usage{
eegBSI(
  x,
  pairs = NULL,
  band = c(1, 25),
  directed = FALSE,
  method = "welch",
  age = NULL,
  montage = NULL,
  reliability = list(icc = NA_real_, sem = NA_real_),
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object with 2D EEG data.}

\item{pairs}{List of length-2 character vectors, each a \code{c(right, left)}
hemispheric electrode pair. Defaults to the standard 10-20 hemispheric
pairs.}

\item{band}{Numeric length-2 frequency band in Hz (default: \code{c(1, 25)}).}

\item{directed}{Logical; when \code{TRUE} the pairwise index keeps its sign
(default: \code{FALSE}).}

\item{method}{Spectral estimator (default: \code{"welch"}).}

\item{age}{Optional numeric age for the normative lookup.}

\item{montage}{Optional character montage for the normative lookup.}

\item{reliability}{Named list of reliability indices to attach (default:
\code{icc} and \code{sem} placeholders).}

\item{assay_name}{Input assay name (default: the default assay).}
}
\value{
A named list with two \code{\link[PhysioCore]{PhysioBiomarker}}
objects: \code{bsi} (global, from 0 to 1) and \code{pdbsi}
(pairwise-derived, signed when \code{directed = TRUE}).
}
\description{
Quantifies left/right spectral asymmetry across hemispheric electrode pairs, a
quantitative-EEG stroke marker (van Putten 2007; Sheorajpanday et al. 2011).
The global \code{bsi} is the spectrum-averaged absolute relative difference
between summed right- and left-hemisphere power, bounded from 0 (symmetric) to
1 (one hemisphere silent). The pairwise-derived \code{pdbsi} averages the same
relative difference per hemispheric pair; with \code{directed = TRUE} it keeps
the sign (positive = right-dominant). Both are returned as
reliability-characterised \code{\link[PhysioCore]{physioBiomarker}} objects
with provenance and, for the global BSI, a normative reference range.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, n_channels = 19, sr = 250)
res <- eegBSI(pe, age = 60)
res$bsi
}
}
\references{
van Putten, M. J. A. M. (2007). The revised brain symmetry index. Clinical
Neurophysiology, 118(11), 2362-2367.

Sheorajpanday, R. V. A., et al. (2011). Quantitative EEG in ischemic stroke:
correlation with functional status after 6 months. Clinical Neurophysiology,
122(5), 874-883.
}
\seealso{
\code{\link[=eegDAR]{eegDAR()}}, \code{\link[=eegAsymmetry]{eegAsymmetry()}}, \code{\link[PhysioCore:physioBiomarker-constructor]{PhysioCore::physioBiomarker()}}
}
