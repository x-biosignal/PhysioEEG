% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-pac.R
\name{eegPAC}
\alias{eegPAC}
\title{Phase-amplitude coupling (cross-frequency coupling) for EEG}
\usage{
eegPAC(
  pe,
  phase_channel = 1L,
  amp_channel = NULL,
  phase_band = c(4, 8),
  amp_band = c(30, 80),
  method = c("tort", "canolty", "ozkurt", "plv"),
  n_bins = 18L,
  assay_name = NULL
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} (continuous data; epoched data is concatenated).}

\item{phase_channel}{Channel (index or label) supplying the modulating phase.}

\item{amp_channel}{Channel supplying the modulated amplitude; \code{NULL} (default)
uses \code{phase_channel} (within-channel PAC).}

\item{phase_band}{Phase frequency band in Hz (default theta \code{c(4, 8)}).}

\item{amp_band}{Amplitude frequency band in Hz (default gamma \code{c(30, 80)}).}

\item{method}{\code{"tort"} (modulation index), \code{"canolty"} (mean vector length),
\code{"ozkurt"}, or \code{"plv"}.}

\item{n_bins}{Number of phase bins for the Tort modulation index (default 18).}

\item{assay_name}{Assay to use (default: the object's default assay).}
}
\value{
The PAC result from \code{PhysioCrossModal::phaseAmplitudeCoupling()} (a
list with \code{pac} and, for \code{"tort"}, the phase-amplitude \code{distribution}).
}
\description{
Measures how the phase of a slow oscillation modulates the amplitude of a fast
oscillation — within a single channel (\code{amp_channel = NULL}) or across two
channels. Delegates the estimator to \code{PhysioCrossModal::phaseAmplitudeCoupling()}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, n_channels = 4, sr = 250)
eegPAC(pe, phase_channel = 1, phase_band = c(4, 8), amp_band = c(30, 80))
}
}
\references{
Tort et al. 2010; Canolty et al. 2006.
}
\seealso{
\code{\link[=eegComodulogram]{eegComodulogram()}}, \code{\link[=eegConnectivityMatrix]{eegConnectivityMatrix()}}
}
