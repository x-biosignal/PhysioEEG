% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-complexity.R
\name{katzFD}
\alias{katzFD}
\title{Katz Fractal Dimension}
\usage{
katzFD(x)
}
\arguments{
\item{x}{A numeric vector (the time series).}
}
\value{
A single numeric value, Katz's fractal dimension.
}
\description{
Computes Katz's fractal dimension (KFD) of a one-dimensional signal
(Katz, 1988) -- a waveform-complexity measure derived from the total length of
the signal curve relative to its extent. Let \eqn{L} be the sum of the
absolute successive differences (the curve length), \eqn{a} their mean, and
\eqn{d} the maximum absolute distance from the first sample; then
\eqn{\mathrm{KFD} = \log_{10}(L/a) / \log_{10}(d/a)}. A smoother, more regular
signal has a lower KFD; a more convoluted (noise-like) waveform a higher one.
}
\details{
This uses the amplitude-only convention adopted by the de-facto reference
implementations (Python \code{antropy}, \code{NeuroKit2}, \code{pyeeg},
\code{mne-features}), in which both the successive distances and the extent are
measured in the signal's amplitude units alone; it reproduces
\code{antropy.katz_fd} and \code{NeuroKit2}'s \code{fractal_katz} bit-for-bit.
(Katz's original definition uses the Euclidean distance in the
sample-index/amplitude plane, which mixes the two axes' units, is
scale-dependent, and is not comparable across tools; the amplitude-only form
is what the field uses and what makes KFD reproducible.)
}
\examples{
katzFD(sin(seq(0, 20 * pi, length.out = 500)))  # low (smooth oscillation)
set.seed(1); katzFD(rnorm(500))                 # higher (noise-like)
}
\references{
Katz, M. J. (1988). "Fractals and the analysis of waveforms."
\emph{Computers in Biology and Medicine}, 18(3), 145--156.
\doi{10.1016/0010-4825(88)90041-8}
}
\seealso{
\code{\link{petrosianFD}} for the derivative-sign-change fractal view,
\code{\link{svdEntropy}}, \code{\link{eegComplexity}} for the multi-measure
wrapper.
}
