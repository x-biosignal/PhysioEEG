% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-preprocess.R
\name{eegBadChannels}
\alias{eegBadChannels}
\title{Detect bad EEG channels}
\usage{
eegBadChannels(
  x,
  method = c("all", "flat", "noise", "correlation"),
  flat_threshold = 1e-06,
  noise_threshold = 4,
  corr_threshold = 0.4,
  assay_name = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{method}{Detection method(s) to apply: \code{"all"} runs all checks,
or specify one or more of \code{"flat"}, \code{"noise"}, \code{"correlation"}.}

\item{flat_threshold}{Variance threshold below which a channel is considered
flat (default: 1e-6).}

\item{noise_threshold}{Number of standard deviations above median variance
to flag a channel as noisy (default: 4).}

\item{corr_threshold}{Minimum mean correlation with other channels. Channels
below this are flagged (default: 0.4).}

\item{assay_name}{Name of the assay to analyze. If NULL, uses
\code{defaultAssay(x)}.}
}
\value{
A data.frame with columns: \code{channel} (label), \code{is_bad}
(logical), \code{reason} (character description), \code{score} (numeric
metric value).
}
\description{
Identifies bad (noisy, flat, or poorly correlated) EEG channels using
multiple automated criteria. Channels flagged as bad can subsequently
be interpolated using \code{\link{eegInterpolate}}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, n_channels = 19, sr = 500)
bad_df <- eegBadChannels(pe)
bad_labels <- bad_df$channel[bad_df$is_bad]
}
}
