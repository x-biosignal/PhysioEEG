% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-ica.R
\name{eegICA}
\alias{eegICA}
\title{EEG Independent Component Analysis (ICA)}
\usage{
eegICA(
  x,
  n_components = NULL,
  method = c("fastica", "infomax", "jade"),
  max_iter = 200L,
  tol = 1e-06,
  assay_name = NULL,
  output_assay = "ica_components"
)
}
\arguments{
\item{x}{A PhysioExperiment object with EEG data.}

\item{n_components}{Number of independent components to extract. Defaults to
the number of channels.}

\item{method}{ICA algorithm: \code{"fastica"}, \code{"infomax"}, or
\code{"jade"}.}

\item{max_iter}{Maximum number of iterations (default: 200).}

\item{tol}{Convergence tolerance (default: 1e-6).}

\item{assay_name}{Input assay name (default: first assay).}

\item{output_assay}{Output assay name (default: \code{"ica"}).}
}
\value{
Modified PhysioExperiment with component activations in
\code{output_assay} and ICA metadata in \code{metadata(x)$ica}.
The ICA metadata list contains: \code{mixing} (mixing matrix A),
\code{unmixing} (unmixing matrix), \code{mean} (channel means),
\code{whiten} (whitening matrix), and \code{method} (algorithm used).
The output assay has dimensions n_time x n_components.
}
\description{
Decomposes multi-channel EEG into independent components using ICA.
Supports FastICA, Infomax, and JADE algorithms. Results are stored in the
output assay (component activations) and in \code{metadata(x)$ica} (mixing
and unmixing matrices).
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, sr = 500)
result <- eegICA(pe, n_components = 4, method = "fastica")
}
}
\references{
Hyvarinen, A., & Oja, E. (2000). Independent component analysis: algorithms
and applications. Neural Networks, 13(4-5), 411-430.

Bell, A. J., & Sejnowski, T. J. (1995). An information-maximization approach
to blind separation and blind deconvolution. Neural Computation, 7(6), 1129-1159.
}
\seealso{
\code{\link[=eegICAremove]{eegICAremove()}}, \code{\link[=eegICAdetect]{eegICAdetect()}}, \code{\link[=eegICAmix]{eegICAmix()}},
\code{\link[=eegFilter]{eegFilter()}}, \code{\link[=eegPreprocess]{eegPreprocess()}}
}
