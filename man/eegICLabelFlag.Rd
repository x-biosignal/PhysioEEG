% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-iclabel.R
\name{eegICLabelFlag}
\alias{eegICLabelFlag}
\title{Flag Artifact ICA Components from ICLabel Probabilities}
\usage{
eegICLabelFlag(
  x,
  prob_threshold = 0.5,
  classes = c("muscle", "eye", "heart", "line_noise", "channel_noise"),
  ica_assay = "ica_components",
  line_freq = 50
)
}
\arguments{
\item{x}{A PhysioExperiment object with ICA results (from \code{\link[=eegICA]{eegICA()}}).}

\item{prob_threshold}{Probability above which a component is flagged
(default: 0.5).}

\item{classes}{Character vector of artifact classes to flag (default: all
non-brain, non-other classes).}

\item{ica_assay}{Metadata name holding the component activations
(default: \code{"ica_components"}).}

\item{line_freq}{Mains line frequency in Hz (default: 50).}
}
\value{
An integer vector of flagged component indices (possibly empty),
with an attribute \code{"labels"} giving the corresponding class of each.
}
\description{
Convenience wrapper over \code{\link[=eegICLabel]{eegICLabel()}} that returns the indices of the
components most likely to be artifacts, ready to pass to \code{\link[=eegICAremove]{eegICAremove()}}.
A component is flagged when its probability in any of the requested artifact
\code{classes} exceeds \code{prob_threshold}.
}
\examples{
\dontrun{
pe <- make_eeg(n_time = 5000, sr = 500)
pe <- eegICA(pe, n_components = 10, method = "fastica")
bad <- eegICLabelFlag(pe, prob_threshold = 0.5)
pe <- eegICAremove(pe, components = bad)
}
}
\seealso{
\code{\link[=eegICLabel]{eegICLabel()}}, \code{\link[=eegICAremove]{eegICAremove()}}, \code{\link[=eegICAdetect]{eegICAdetect()}}
}
