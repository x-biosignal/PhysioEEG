% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eeg-source.R
\name{eegDipoleFit}
\alias{eegDipoleFit}
\title{Fit an equivalent current dipole (ECD) to an EEG topography}
\usage{
eegDipoleFit(
  x,
  time = NULL,
  method = c("spherical", "bem_simplified"),
  n_grid = 8L,
  assay_name = NULL
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}.}

\item{time}{Sample index to fit (a single index), a length-2 range to average
over, or \code{NULL} (default) to fit the peak global-field-power sample.}

\item{method}{Forward physics: \code{"spherical"} (single-sphere) or
\code{"bem_simplified"} (Berg-Scherg 3-shell).}

\item{n_grid}{Grid resolution per axis for the initial search (default 8).}

\item{assay_name}{Assay to use (default: the object's default assay).}
}
\value{
An \code{eeg_dipole_fit} object: \code{position} (x, y, z in normalized head
radius), \code{moment}, \code{orientation} (unit), \code{amplitude}, \code{gof} (goodness of
fit, variance explained), \code{residual}, and the fitted/observed topographies.
}
\description{
Localizes the single equivalent current dipole that best explains a scalp
topography, by nonlinearly optimizing the dipole position (a grid search
inside the head sphere refined with Nelder-Mead) while the dipole moment is
the closed-form least-squares fit at each candidate position. Uses the same
forward physics as \code{\link[=eegForwardModel]{eegForwardModel()}} and the electrode positions from
\code{colData} / the montage. This is the classic focal-source complement to the
distributed inverses in \code{\link[=eegSourceEstimate]{eegSourceEstimate()}}.
}
\examples{
pe <- make_eeg(n_time = 100, n_channels = 19, sr = 100)
fit <- eegDipoleFit(pe)
fit$gof
}
\references{
Scherg (1990); Mosher, Lewis & Leahy (1992).
}
\seealso{
\code{\link[=eegSourceEstimate]{eegSourceEstimate()}}, \code{\link[=eegForwardModel]{eegForwardModel()}}
}
